# L1 STATUS — Honest Assessment (2026-09-07)

> This document replaces marketing fluff with technical reality. Every claim here is verifiable.

---

## 🟢 WORKING (tested, builds, tests pass)

| Component | Status | Tests | Notes |
|-----------|--------|-------|-------|
| **crypto/** (`quanta-l1-crypto`) | ✅ Builds & tests pass | 10/10 | Pure-Rust Dilithium3 wrapper (dilithium-rs v0.2). `generate_keypair()` uses real OS entropy (getrandom). Uniqueness test added: `test_keypair_uniqueness` fails if stub returns zeros. |
| **pallet-pq-dilithium** | ✅ Builds & tests pass | 7/7 | AccountId = DilithiumPublicKey; signature verification via `Verify` trait. |
| **pallet-pq-balances** | ✅ Builds & tests pass | 6/7* | Basic balances pallet. *1 test flaky (see below). |
| **runtime** | ✅ Builds | 2/2 | Minimal mock runtime for pallet integration tests. |

---

## 🟡 PARTIAL / NEEDS WORK

| Component | Status | Blockers |
|-----------|--------|----------|
| **pallet-pq-staking (PoUW)** | ⚠️ Scaffold only — **NON-FUNCTIONAL** | See detailed breakdown below. |
| **node** | ⚠️ Skeleton only — **NO SERVICE** | `main.rs` ~7 lines; no chain_spec, CLI, RPC, networking, block authoring. |
| **stubs/getrandom-03** | ⚠️ Disabled globally | Was patched globally (returned zeros). Now **commented out** in workspace Cargo.toml — native uses real getrandom; wasm32 target disabled in crypto/Cargo.toml. |

---

## 🔴 NOT WORKING / MOCK

| Area | Reality |
|------|---------|
| **Consensus** | No Aura/GRANDPA running. No block production. No finality. |
| **P2P / Networking** | `sc-network` stubbed; no actual libp2p connections. |
| **RPC** | No JSON-RPC server. No `eth_*` or `system_*` endpoints. |
| **Chain spec** | File exists (`CHAIN_SPEC.md`) but no `chain_spec.rs` — cannot generate genesis. |
| **WASM runtime** | Not built; wasm32 target commented out. |

---

## 📦 pallet-pq-staking — DETAILED GAPS

> **File header now states:** "⚠️ NON-FUNCTIONAL SCAFFOLD: no fund locking, no proof verification, no payouts. DO NOT present as working PoUW."

| Missing Piece | Current Code | Required for Production |
|---------------|--------------|-------------------------|
| **Fund locking** | `stake()` only writes to `StakeInfo` map. No `Currency` trait, no `reserve`/`unreserve`, no balance transfer. | Implement `Config: pallet_balances::Config` + `T::Currency::reserve()` / `unreserve()` / `repatriate_reserved()`. |
| **Proof verification** | `submit_inference(model_hash, input_hash)` ignores both params; just increments counter + adds fixed reward. | Integrate ZK verifier / PoW checker. Reward only on `verify_proof(model_hash, input_hash, witness) == true`. |
| **Actual payouts** | `claim_reward()` clears `PendingRewards` to 0 — **no token transfer**. | `T::Currency::transfer()` or `mint()` to caller. Add inflation config. |
| **Slashing / Economics** | None. No slashing, no commission, no eras/sessions, no validator set. | Full PoS/PoUW economics: era rotation, commission, slash on equivocation/invalid proof. |
| **Tests** | Mock runtime only (11 tests pass). | Integration tests against real chain; fuzzing; formal verification of reward logic. |

---

## 🧪 TEST INVENTORY (as of 2026-09-07)

| Crate | `cargo test -p <crate>` | Notes |
|-------|-------------------------|-------|
| `quanta-l1-crypto` | 10 passed | Includes new `test_keypair_uniqueness` |
| `pallet-pq-dilithium` | 7 passed | |
| `pallet-pq-balances` | 6 passed, 1 flaky | Flaky: `test_transfer_insufficient_balance` (timing/order) |
| `pallet-pq-staking` | 11 passed | Mock only — tests scaffold logic, NOT real behavior |
| `runtime` | 2 passed | |
| **Total (workspace, excluding node)** | **36 passed** | |

> To run: `cd l1 && cargo test -p quanta-l1-crypto -p pallet-pq-dilithium -p pallet-pq-balances -p pallet-pq-staking -p quanta-l1-runtime`

---

## 🎯 NEXT STEPS (priority order)

1. **Node service** — Implement `chain_spec.rs`, `service.rs`, `cli.rs`, `rpc.rs` with manual-seal for devnet.
2. **Staking rewrite** — Add `Currency` trait, proof verification interface, actual payouts, economics.
3. **Enable wasm32** — Uncomment wasm32 target in crypto/Cargo.toml, fix getrandom stub for wasm only.
4. **Local testnet** — 2+ nodes, block production, RPC reachable.
5. **Audit** — Crypto + staking economic model before any value at stake.

---

*Generated by PROMPT 12 execution. Update this file when reality changes.*